<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
  <a class="nav-link" href="{{ path('index') }}"><img src="{{ asset('/images/logo.png') }}" alt="image-accueil" class="img-fluid navbar-logo" /></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    Menu
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <!-- Centre les éléments de nav -->
    <ul class="navbar-nav mx-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('index') }}">Accueil</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_annonce_index') }}">Toutes les annonces</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_annonce_new') }}">Ajouter une annonce gratuitement</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Vidéos</a>
      </li>
      <div class=" d-md-none d-block">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_account') }}">Mon compte <i class="fa-regular fa-circle-user ml-2"></i></a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
          </li>
        {% endif %}
      </div>
    </ul>

    <!-- Liens de droite qui se déplacent en colonne sur mobile -->
    <ul class="navbar-nav user-nav ml-auto mt-3 mt-lg-0 d-none d-md-flex">
      {% if app.user %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_account') }}">Mon compte <i class="fa-regular fa-circle-user ml-2"></i></a>
        </li>
      {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
        </li>
      {% endif %}
    </ul>
  </div>
</nav>
